//! The multi-run dashboard (route `/`). Stub for now; filled in with the
//! runs overview and stats in a later task.

use leptos::prelude::*;

#[component]
pub fn Dashboard() -> impl IntoView {
    view! {
        <div class="dashboard-view">
            <h1>"Dashboard"</h1>
        </div>
    }
}
